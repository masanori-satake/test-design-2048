# テスト設計ワークシート（検討用）

* **氏名**: ____________________
* **作成日**: 20____年 ____月 ____日
* **対象リポジトリ**: `test-design-2048`

---

## テスト設計 1: 同値分割・境界値分析ワークシート
**対象機能**: `src/board_logic.py` (`slide_and_merge_line`, `is_board_full`)

### 1-1. 同値クラス（Equivalence Class）の整理
テスト対象の入力条件を整理し、有効同値（期待通りの動作）と無効同値（エラー・例外動作）に分類してください。

| 条件識別子 | 対象入力・入力パラメータ | 有効同値クラス（Valid） | 無効同値クラス（Invalid） |
| :--- | :--- | :--- | :--- |
| **C1** | 配列の要素数 | `[                  ]` | `[                  ]` |
| **C2** | 配列の要素（数値） | 0, `[                  ]` | 負の数, 奇数, `[                  ]`（※現行実装では値未検証） |
| **C3** | 結合可能な組み合わせ | なし, `[                  ]` | - |

> 💡 **ヒント**:
> - 配列の長さをチェックする関数の仕様から、正常となる要素数と例外となる要素数を検討しましょう。
> - タイルの数値として許可される値（0および2の累乗）とそれ以外の無効な値（負数、奇数など。※現行の `slide_and_merge_line` では要素の数値検証を行っていない点にも注意）を整理しましょう。
> - 1行における結合可能なペアのパターン（なし、1ペア、2ペアなど）を分類しましょう。

---

### 1-2. 境界値（Boundary Value）の整理
仕様上の閾値・条件の境目となる値を特定してください。

| 条件項目 | 下限境界（On/Off） | 代表値（In） | 上限境界（On/Off） | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| スコア計算値 | `[                  ]` （合成なし） | `[                  ]` （2+2合成） | `[                  ]` （1024×4から2048×2合成） | 1行(長さ4)での1スライド最大獲得スコア |
| 空きマスの数 | `[                  ]` （満杯） | `[                  ]` | `[                  ]` （全空き） | `is_board_full` |

> 💡 **ヒント**:
> - スコア計算では、1スライドで合成が発生しない最小値（0点）から1行（長さ4）で獲得できる最大値の境界値を考えましょう。
> - 盤面（4×4=16マス）の空きマス数における境界（最小0個＝満杯、最大16個＝全空き）と代表的な範囲を整理しましょう。

---

### 1-3. テストケース設計表
同値クラスと境界値をカバーするテストケースを作成してください。

| テストID | テスト目的 / 観点 | 入力値 (`line`) | 期待出力 (`new_line`, `score`) | 網羅した条件ID |
| :--- | :--- | :--- | :--- | :--- |
| **TC-S1-01** | 移動も合成もない状態 | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-02** | 隙間がある場合のスライドのみ | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-03** | 隣り合う同じ数値の合成 | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-04** | 連鎖しないことの検証（1移動1合成） | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-05** | 左優先合成の検証 | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-06** | 離れた同じ数値の合成 | `[                  ]` | `[                  ]` | `[                  ]` |
| **TC-S1-07** | 無効同値：配列長エラー | `[                  ]` | `[                  ]` | `[                  ]` |

#### `is_board_full` 用テストケース

| テストID | テスト目的 / 観点 | 入力値 (`board`) | 期待出力 (`is_board_full`) | 備考 |
| :--- | :--- | :--- | :---: | :--- |
| **TC-S1-08** | 満杯盤面の判定 | `[                  ]` | `[                  ]` | 空きマスの数: 0個 |
| **TC-S1-09** | 空きマスあり盤面の判定 | `[                  ]` | `[                  ]` | 空きマスの数: 1個 |

> 💡 **ヒント**:
> - 各テスト観点に対応する `line` の具体例（例: `[0, 0, 0, 0]`, `[2, 2, 0, 0]` など）を考案してください。
> - 期待出力は `(スライド後の配列, 獲得スコア)` の形式や例外発生（`ValueError`）を指定しましょう。

---

## テスト設計 2: 組合せテスト（デシジョンテーブル / ペア構成法）ワークシート
**対象機能**: `src/game_manager.py` (`is_game_over`)

### 2-1. 因子と水準の抽出
ゲームオーバー判定に影響を与える条件（因子）と、その取り得る値（水準）をあげてください。

| 因子（Factor） | 説明 | 水準1（Level 1） | 水準2（Level 2） |
| :--- | :--- | :--- | :--- |
| **F1: 空きマス** | 盤面内に `0` が存在するか | 存在する (`[                  ]`) | 存在しない (`[                  ]`) |
| **F2: 水平可動** | 左右に隣接する同じ数字があるか | ある (`[                  ]`) | ない (`[                  ]`) |
| **F3: 垂直可動** | 上下に隣接する同じ数字があるか | ある (`[                  ]`) | ない (`[                  ]`) |

> 💡 **ヒント**:
> - 条件の有無に応じた真偽値（`True` / `False`）を各因子の水準として定義しましょう。

---

### 2-2. デシジョンテーブル（意思決定表）
因子のすべての組合せと、それに対する期待動作（Game Over判定）を整理してください。

| 条件 / ルール | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **F1: 空きマスが存在する** | Y | Y | Y | Y | N | N | N | N |
| **F2: 水平方向に合成可能** | Y | Y | N | N | Y | Y | N | N |
| **F3: 垂直方向に合成可能** | Y | N | Y | N | Y | N | Y | N |
| **【期待値】Game Overか？** | `[                  ]` | `[                  ]` | `[                  ]` | `[                  ]` | `[                  ]` | `[                  ]` | `[                  ]` | `[                  ]` |

> **設計メモ・考察（なぜこの期待値になるか）**:
> 空きマスが1つでもあれば、その時点でゲーム継続（`[                  ]`）。
> 空きマスがなくても（`F1=N`）、水平または垂直に動かせるならゲーム継続（`[                  ]`）。
> 全て動かせない（`R8`）の場合のみゲームオーバー（`[                  ]`）となる。

> 💡 **ヒント**:
> - 2048のルール上、ゲームオーバー（`True`）になるのは「空きマスがなく、かつ縦横どちらにも合成できるタイルが存在しない場合（R8）」のみです。

---

### 2-3. テストケース（盤面データ設計）

| テストID | 対応ルール | 代表的な盤面イメージ (4x4) | 期待出力 (`is_game_over`) |
| :--- | :---: | :--- | :---: |
| **TC-S2-01** | R1 | 空きマスあり・`[                  ]` | `[                  ]` |
| **TC-S2-02** | R2 | 空きマスあり・水平合成可・`[                  ]` | `[                  ]` |
| **TC-S2-03** | R3 | 空きマスあり・水平合成不可・`[                  ]` | `[                  ]` |
| **TC-S2-04** | R4 | 空きマスあり・`[                  ]` | `[                  ]` |
| **TC-S2-05** | R5 | 空きマス0・`[                  ]` | `[                  ]` |
| **TC-S2-06** | R6 | 空きマス0・水平合成可・`[                  ]` | `[                  ]` |
| **TC-S2-07** | R7 | 空きマス0・水平合成不可・`[                  ]` | `[                  ]` |
| **TC-S2-08** | R8 | 空きマス0・`[                  ]` (市松模様等で一切合成不可) | `[                  ]` |

> 💡 **ヒント**:
> - 各ルール（R1〜R8）を再現する盤面条件と、それに対する `is_game_over` の戻り値（`True` または `False`）を記入しましょう。

---

## テスト設計 3: 状態遷移テストワークシート
**対象機能**: `src/tile_generator.py` (`spawn_tile`), ゲーム進行状態

### 3-1. 状態遷移図 / 状態遷移表の設計

#### 状態一覧
* **S0**: 空盤面（初期化直後、全16マス空き）
* **S1**: ゲーム開始状態（`spawn_tile` `[                  ]` 回実行後、初期タイル2つ配置済み）
* **S2**: 途中盤面（空きマス `[                  ]` 個）
* **S3**: 満杯盤面（空きマス0・`[                  ]`）
* **S4**: 詰み盤面（空きマス0・`[                  ]`＝Game Over）

#### 状態遷移表（State Transition Table）

| 現状態 \ イベント | ①操作入力(移動発生) | ②操作入力(移動不可) | ③`spawn_tile` 実行 |
| :--- | :--- | :--- | :--- |
| **S0: 空盤面** | N/A | N/A | `[                  ]` で S1 へ遷移 |
| **S1: 開始(2枚)** | `[                  ]` | S1のまま | `[                  ]` (タイルの追加) |
| **S2: 途中** | `[                  ]` | S2のまま | `[                  ]` |
| **S3: 満杯(合成可)**| `[                  ]` (合成で空き発生)| S3のまま | `[                  ]` (生成されない) |
| **S4: 詰み(GameOver)**| N/A (操作不可) | N/A | N/A |

> 💡 **ヒント**:
> - 各状態から「移動発生」「移動不可操作」「タイル生成」などのイベントが起きた時、どの状態（S0〜S4）に遷移するかを追跡しましょう。

---

### 3-2. カバレッジレベルの設定とテストケース

テストの網羅性レベルを選択し、対応するテストシークエンスを作成してください。
* [ ] **0-switch カバレッジ**: すべての状態・イベントの組み合わせを通る（単一遷移）
* [ ] **1-switch カバレッジ**: 2回連続する遷移のペア（S_a $\rightarrow$ S_b $\rightarrow$ S_c）をすべて通る

| テストID | テスト観点 / 遷移パターン | シナリオ手順（操作・状態変化のフロー） | 期待結果 |
| :--- | :--- | :--- | :--- |
| **TC-S3-01** | 満杯状態からの復活 | S3 (空き0/合成可) $\rightarrow$ 移動入力 $\rightarrow$ `[                  ]` | `[                  ]` |
| **TC-S3-02** | 変化なし移動でのタイル不生成 | S2 (移動不能方向へ入力) $\rightarrow$ `[                  ]` | `[                  ]` |
| **TC-S3-03** | 限界状態（Game Over遷移）| S2 (残り空き1マス) $\rightarrow$ 移動入力 $\rightarrow$ `[                  ]` で `spawn_tile` の生成位置と値(2)を固定して最後の空きマスへ生成 $\rightarrow$ `[                  ]` $\rightarrow$ S4 (詰み) | `is_game_over(board)` が `[                  ]` となり Game Over 状態に遷移すること |

> 💡 **ヒント**:
> - タイル合成による状態変化、盤面変化なしで `spawn_tile` が呼ばれない挙動、および `unittest.mock.patch` で生成結果を固定した詰み状態（S4）への遷移を確認しましょう。
